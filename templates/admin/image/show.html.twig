{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table table-striped projects">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ image.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ image.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_image_index') }}">Revenir sur la liste</a>

    <a href="{{ path('admin_image_edit', {'id': image.id}) }}"><edit></a>

    {{ include('admin/image/_delete_form.html.twig') }}
{% endblock %}
