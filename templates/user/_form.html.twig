{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    </br>
    <div class="col-md-4">
        <button class="btn btn-block btn-primary cold-md-auto">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
